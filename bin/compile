#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

# parse and derive params
BUILD_DIR=$1
CACHE_DIR=$2
ENV_DIR=$3


echo "**Insalling mediasoup requirements**"
apt-get -o debug::nolocking=true -y install python3-pip build-essential


echo "__Installation complete__"
